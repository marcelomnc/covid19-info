import React from "react";
import NavigationBarBS from "./NavigationBarBS/NavigationBarBS";

const Header = (props) => {
	return (
		<header className="Header">
			<NavigationBarBS />
		</header>
	);
};

export default Header;
